---
apiVersion: v1
kind: Service
metadata:
  name: mongo-database-service
  namespace: quizmeup
  labels:
    app: mongo-database
spec:
  type: ClusterIP
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
  selector:
    app: mongo-database