apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: quizmeup
  name: administration-server-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: administration-server.quizmeup.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: administration-server-service
                port:
                  number: 8080