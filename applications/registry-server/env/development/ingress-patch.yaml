apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: quizmeup
  name: registry-server-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: registry-server.quizmeup.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: registry-server-service
                port:
                  number: 8080